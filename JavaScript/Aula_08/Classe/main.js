// nota = 0 seta o valor default
function mediaAluno (nota1, nota2) {
    return (nota1 + nota2) /2;
} 
 
function mediaAluno(nota1 = 0, nota2 = 0) {
    return (nota1 + nota2) /2;
} 
let falta = 2

function name(params) {
    
}
let nt1 = Number(prompt("Nota aluno(a)"))
let nt2 = Number(prompt("nota aluno(a)"))

alert(mediaAluno(nt1, nt2));

 